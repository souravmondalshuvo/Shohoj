# Migration Roadmap

> Date: 2026-06-28. Incremental **strangler** migration — never a big-bang rewrite.
> `build3.py` + GitHub Pages remain the rollback path until the Vite/React app passes
> full parity. One branch / one PR per coherent phase. Reconciles (does not replace)
> `docs/FULL_REACT_TYPESCRIPT_MIGRATION.md`.
>
> **Statuses reconciled against the repository on 2026-09-08 (#656).** Every claim in
> the Status column below is checkable from a file, a CI step or a merged PR; where a
> phase is partial the row names what specifically remains.

| Phase | Goal | Exit criteria | Status |
|---|---|---|---|
| **0** | Audit & reconciliation | This doc set exists; current state verified | ✅ done (2026-06-28) |
| **1** | Stabilize the production build | Collision guard in CI; all unsafe collisions fixed; bundle regression coverage | ✅ **done** — `scripts/check_bundle_collisions.py` runs in CI as `npm run check:collisions`, ahead of `build3.py`; the eight unsafe `MAIN` collisions were namespaced; `npm run test:bundle` (production-bundle smoke) and `npm run test:csp` (inline-handler guard) hold the line beside it. |
| 2 | Shared foundations | Strict TS config; error/result/schema/logging/flags/config-boundary primitives; dependency-boundary lint; small UI primitives (no CSS rewrite) | ✅ **done** — dependency-boundary lint, zod→Result validation, structured logger, typed feature flags, typed runtime-config boundary, stricter TS flags (noFallthroughCasesInSwitch / forceConsistentCasingInFileNames / noImplicitOverride), and the React-semesters island opt-in wired through the flag registry. Deferred (with rationale): **UI primitives → Phase 3** (no real consumers until the React shell exists); **stricter flags** exactOptionalPropertyTypes / noPropertyAccessFromIndexSignature / noUncheckedIndexedAccess → land with Phase 5/6 (fixes touch React prop contracts + the feed client). **Still outstanding (2026-09-08):** those three flags never landed. Phases 5 and 6 are now closed, so the deferral has no phase left to ride on — `tsconfig.json` still carries `"strict": true` and nothing more. It is tracked as #658 now rather than handed to another phase, with the cost measured per flag: 15 errors for `exactOptionalPropertyTypes`, 117 for `noPropertyAccessFromIndexSignature`, 213 for `noUncheckedIndexedAccess`. |
| 3 | React app shell | RR Framework Mode; layouts; route error boundaries; auth/theme/notification/modal/config providers; nav; protected admin; lazy routes; **small UI primitives** (deferred from Phase 2 — build alongside their first real consumers) | ✅ **done** — SPA React Router shell skeleton landed: isolated `vite.shell.config.js` → `dist-shell/` (never touches build3.py or the islands), root layout + provider stack + nav + skip link, route-level error element, lazy/code-split routes (Home, Calculator placeholder, 404). Added: typed AuthProvider (useSyncExternalStore + injectable source port; pure snapshot normalization, unit-tested), RequireAdmin client-side guard (server-side authz stays the real boundary), full route map (14 routes, placeholders for unmigrated features) + nav. Added: ModalProvider (typed, promise-based `useConfirm`, focus-trapped + Escape/backdrop, aria-modal), RuntimeConfigProvider (`useRuntimeConfig`/`useCapabilities` over the Phase 2 config boundary; offline degradation, unit-tested), first `shared/ui` primitive (Button) consumed by the dialog + Home. Provider stack complete. Notification viewport landed (#311, this PR): `NotificationViewport` in the root layout renders the provider's items as accessible toasts (always-mounted live region; status role for success/info/warning, alert for sticky errors; labelled dismiss) — first consumer is the demo-mode success toast. Remaining: RR Framework-Mode (loaders/actions) per route — lands with real data routes in Phase 5/6 **Closed:** the provider stack, nav, route error element and the lazy route map all ship, and the `shared/ui` primitives deferred from Phase 2 arrived with their consumers. **Deviation recorded, not pending work:** the shell never adopted RR Framework-Mode loaders/actions — routes are `lazy` modules reading through providers and bridges, because the data sources are `localStorage`, the Worker and live Firestore subscriptions rather than request-time fetches. There are no `loader:`/`action:` definitions in `src/app/routing/`, by choice. |
| 4 | State & persistence | Wire versioned local-data migration into the live path; pre-migration backup; corrupt-state recovery; preserve cloud/local conflict + first-sign-in guards; never overwrite academic data | ✅ **done** — safe persistence engine built + heavily tested: guarded `localStorage` adapter (degrades to memory), composed `loadAcademicState`/`saveAcademicState` over the existing backup + versioned-migrate + validate pieces, encoding the safety policy (backup-once before migrate; corrupt/unusable → recover to empty but NEVER overwrite the stored raw; load never writes; save stamps the schema version). Reuses the existing forward-compat (unknown-field-preserving) migrate/sync-decision/backup modules. Pending (needs a live consumer + sign-off): wiring into the calculator route at Phase 5 cutover, multi-device/cloud-conflict end-to-end, and not touching the live legacy JS until then **Closed:** wired into the live shell `/calculator` route with the 5A/5B slices, so the engine has the consumer it was waiting for. Cloud/local conflicts resolve by fingerprint and explicit user choice in `src/services/cloudSync` — never newest-wins — and the sign-out device wipe is covered by the drift-guarded key list held in both `js/core` and `src/services`. |
| 5 | Calculator cutover | Full parity (semesters, summary, edit, autocomplete, retakes, statuses, import, planner, demo, PDF, cloud restore, mobile, a11y); drop legacy globals; default only after unit+component+vite-e2e+bundle pass and rollback intact | ✅ **done** — **5A/5B** landed: pure reducer + mutations + Phase-4 persistence wired into the shell `/calculator` route; `CalculatorSemesters` renders through an injected `CalculatorBridge` (island keeps the legacy window bridge). **5C** landed: real typed BRACU catalogue + accessible WAI-ARIA combobox autocomplete on the shell route — see Phase 5C detail. **5D** landed: pure CGPA results model + bridge-driven results section (headline, meter, standing, incomplete warning, credit totals) on the shell route; the three results islands now share the same model — see Phase 5D detail. **Add-semester controls** (#307) landed: footer + Add Semester / 🎯 Running Semester on the shell via the bridge, calendar-aware naming in pure `semesterNaming.ts` (clock injected at dispatch; single-running reducer guard; documented deviation — no department picker yet, so default Spring/Summer/Fall calendar and Semester-N / Current-Semester fallbacks without a start). **Demo mode** (#309) landed: Try Demo Mode fills the typed legacy-parity dataset (`demoData.ts` — Fall 2024/Spring 2025, start Fall 2024) through the reducer's `replace`, confirming via the shell modal before overwriting existing data; the success toast landed with the notification viewport (#311). **Dept/start setup** (#313) landed: typed department boundary (`js/core/departments.d.ts` + `departments.ts` adapter over the single legacy `DEPARTMENTS` source), setup controls on the route (dept select + credits chip + calendar-scoped season/year), `currentDept` in reducer state + persistence, naming honours the department calendar (PHR/LAW two-season math), demo pre-selects CSE, and the persist path now spreads the loaded snapshot first so fields the route does not own (planCourses, semesterCounter, forward-compat keys) survive a save. **Dashboard parity** (#315) landed: pure `degreeProgress.ts` (tracker.js parity — visibility, per-semester credits, summary estimation, pace, graduation walk, projected labels + more-count, complete state; injected clock) + pure `gpaTrend.ts` (recalc trend parity + drawTrendChart geometry) rendered as the bridge-driven `DegreeTracker` and the SVG `GpaTrendChart` (typed replacement for the canvas) in the results section; `CalculatorInputs` gains `currentDept`. **Simulator + retake strategy** (#317) landed: pure `simulator.ts` mirrors runSimulator + buildRetakeSuggestions (validation order, secured/no-credits split, needed-GPA formula, difficulty/insight tiers, 7-tier letter map, 9/12/15 plan rows, all-A ceiling, retake/repeat candidates top-6 by boost-to-B, stacked-selection impact); `CgpaSimulator` renders it below the results with the legacy dataset.auto + activeElement rule for the dept-driven remaining-credits auto-fill; summary-only data gets the nudge (transcript-import button deferred with that slice). **Faculty rating** (#319) landed: pure `reviewDraft.ts` mirrors openReviewModal's form (the five rating dimensions with labels/hints, live initials normalization, the 500-char note cap, the legacy validation order + messages, payload shaping) and `reviewSubmit.ts` ports submitReview's orchestration environment-injected (validateReview's order with catalog membership as a predicate, the signed-out 'Sign in to submit a review' refusal, clamps, on-success faculty-cache upsert), reading the same `_shohoj_submitReview`/`_shohoj_currentUid` window hooks — so the standalone shell degrades exactly like a signed-out legacy session and a signed-in page submits unchanged; `RateFacultyModal` (shell dialog conventions: focus trap/restore, Escape/backdrop close) opens from the semester rate pill/chip via the bridge, and an ok submit writes the normalized initials onto the course (the chip renders) plus the success toast. Deferred to the typed Firebase boundary (5G/6): the existing-review probe and live chip score aggregates (the shell chip keeps the '–' placeholder). **Transcript import** (#323) landed: typed `transcriptPdfText.ts` (the modals.js positional flattener — y-jump rows, dropped-course-code + all-caps-overflow newlines, x-gap/token-shape spaces) + pure `transcriptImport.ts` (importTranscriptPDF/applyImport orchestration: the line-vs-blob fallback rule, catalogue cleanup via injected lookup incl. the F(NT) 3-credit default, identity detection, and the state mapping — sequential ids, dept label→code, legacy start season/year rules) + `pdfjsLoader.ts` (the same pinned CDN 3.11.174 + SRI, loaded on demand, `isEvalSupported:false`; existing `window.pdfjsLib` short-circuits) + `TranscriptImport` (hidden picker behind an imperative handle — the shell analogue of the one hidden #transcriptFileInput — with reading/confirm/could-not-parse/failed dialogs); triggers: calc footer, empty state, and the simulator nudge's now-unlocked 📄 button via `bridge.importTranscript()`. Deferred with the profile feature: applyImport's identity → Profile snapshot write. **PDF export** (#325) landed: pure `pdfReport.ts` (exportPDF's pre-draw computations from CalculatorState + injected clock: retake-aware non-running totals via `calculateCgpaTotals`, legacy standing strings/colour tiers, semester count with the now-exported degreeProgress summary estimator, section/row displays incl. the F(NT) '0.00', notes, retaken flags, footer math, filename + date format) + `pdfReportDraw.ts` (the verbatim jsPDF layout walk over a structural doc type — palette, header/stat band/summary card/tables/badges/zebra/checkY pagination/footer) + `jspdfLoader.ts` (pinned CDN 2.5.1 + SRI on demand; existing `window.jspdf` short-circuits — the test seam); the footer ⬇ Export PDF button wires it, failures toast the legacy copy instead of alert(). **Planner** (#327) landed: `/planner` becomes a real lazy route over the already-typed engine (src/core/planner.ts): `COURSE_DB` + `PREREQS` join the catalogue boundary (declaration + typed re-exports), pure `plannerTotals.ts` ports the playground's getCurrentTotals (the impact preview's base — running included, F(NT)/P/I skipped; deliberately not calculateCgpaTotals), and `planCourses` becomes reducer-owned CalculatorState (load filter + explicit persist; addPlanCourse deduped / removePlanCourse / clearPlan / atomic promotePlan replacing any running semester and emptying the plan). Plan resets for parity: transcript import (closing the #324 deviation), setDept (onDeptSelect parity, ''-keeps-it), and demo mode seeds the legacy DEMO_PLAN_COURSES. PlannerRoute renders the legacy tab's surface — empty states, stats strip, plan rows with prereq notes, 9–15 credit bands, per-grade CGPA impact preview, prereq trees, search + all/unlocked/locked filters with the 25-row cap, and validation-gated promote (confirm-replace, then navigate to /calculator). Deferred (documented): the ⭐ review buttons + Browse Reviews (Firestore reads → the reviews/Firebase slice) and the nav plan-count badge. **Runtime config into the shell (5G-a)** (#329) landed: a vite.shell.config.js plugin serves/emits the generated, gitignored js/config/runtime-config.js as /runtime-config.js (comment stub when absent — fresh clones/CI build and stay cleanly offline); app/index.html loads it before the module entry; RootLayout feeds runtimeConfigFromGlobals(window) into RuntimeConfigProvider, so a valid generated config lights `useCapabilities().cloud` and anything less degrades offline. First step of the typed Firebase boundary (5G) — decision recorded: the shell's Firebase SDK will be the npm package as a lazy Vite chunk (not the legacy gstatic CDN), promoted from devDependencies with the clean-reinstall lockfile procedure. **Firebase auth source (5G-b)** (#331) landed: `firebase` promoted to a runtime dependency (clean-reinstall lockfile procedure, npm ci --dry-run verified); `src/platform/firebase/firebaseClient.ts` is the lazy dynamic-import singleton (own Vite chunks — the offline shell never requests them, e2e-asserted) initialised from the VALIDATED config with best-effort App Check; `createFirebaseAuthSource` implements the AuthSource port over an injectable backend with the legacy BRACU domain enforcement as a pure decision (verified @g.bracu.ac.bd Google account or admin claim, else sign-out + 'rejected' event), isAdmin from the token claim, popup sign-in with the user-closed swallow; ShellChrome builds the source inside RuntimeConfigProvider and the header shows checking/sign-in/email+sign-out, toasting rejection/failure with the legacy copy. Deferred (documented): Google One Tap (GIS signInWithCredential). **Cloud restore/conflict (5G-c)** (#333, this PR): typed `userSync.ts` (parseStoredState + getDataFingerprint, byte-parity ported), `userDocRepo.ts` (typed users/{uid} repo — the exact legacy `{data: JSON string, updatedAt: serverTimestamp}` shape, merge writes, injectable Firestore surface), and `cloudSyncEngine.ts` — the I/O orchestrator driven entirely by the pre-existing `syncDecision.ts` pure functions, adding only the 700ms debounce/queue, the 5s own-write grace clock and the session flags. A local-save announcer (typed `_shohoj_onSave` analogue) forwards persistCalculatorState's successful writes to the debounced cloud save; CloudSyncProvider runs the engine for a signed-in cloud shell (start on uid / stop on sign-out), renders the forced-choice migration dialog (no dismiss, legacy parity), and applies cloud data by reload (the shell rebuilds from storage on boot — in-place apply deferred). Conflicts resolve by fingerprint + explicit user choice, never newest-wins. 16-scenario engine battery + repo/userSync unit tests; e2e asserts the signed-out shell is unaffected (live sign-in can't run in CI). Deferred (documented): in-place apply without reload; Worker-side token verification stays Phase 7. Remaining 5G: the typed reviews repo (unlocks the deferred chip scores, existing-review probe and planner review buttons). Then: full mobile + final a11y parity, production cutover **Closed:** the remaining 5G work landed (the typed reviews repo, read *and* write, which unlocked the chip score aggregates, the existing-review probe and the planner review buttons), followed by full mobile and a11y parity and a full-shell E2E gate in CI. The one exit criterion still open — "default only after…" — *is* the production cutover, tracked as Phase 13 rather than here. |
| 6 | Feature migration (risk order) | transcript → planner/degree-progress → simulator/playground → routine → seats/watchlist → free-rooms → reviews/difficulty → papers → groups → feedback → profile → admin | ✅ **done** — every name in the risk order is a real lazy route in `src/app/routing/router.tsx` with a spec in `e2e-shell/`: transcript, planner, degree-progress, playground, routine, seats, rooms, difficulty, reviews, papers, groups, feedback and profile — plus campus, bus, lost-found and cafeteria, which did not exist when this row was written. Routes are campus-scoped through `campusRoute()`. **Deviation recorded:** admin is not a shell route — it stays the standalone `admin.html` and the nav links out to it (decided in #460). |
| 7 | Firebase & server boundaries | Typed init/Auth adapter/Firestore repos; server token verification; preserve App Check, rules, deterministic review IDs, review immutability, paper owner paths, admin claims; Firebase failure can't break offline calculator | ✅ **done** — nine typed repos in `src/platform/firebase/` (user doc, reviews read + write, papers, groups, feedback, lost-found, seat alerts, campus stamp); `createFirebaseAuthSource` over an injectable backend, with the SDK in its own lazy chunk the offline shell never requests (e2e-asserted); the Worker verifies Firebase ID tokens itself (`jwtVerify` + `createRemoteJWKSet`, `worker/index.js`); App Check stays best-effort; deterministic review IDs and review immutability are enforced in `firestore.rules`. **Caveat — an ops failure, not a phase gap:** the rules in this repo may not be the rules Firestore is enforcing, because the deploy job has been failing (#651). |
| 8 | Cloudflare full-stack | Cloudflare Vite integration; sensitive ops behind Worker routes/actions; preserve R2 + seat-alert cron; typed env bindings; local Worker dev/tests; keep current Worker deployable; no duplicated business rules | 🟡 **in progress** — the sensitive operations are already Worker-side (`/upload`, `/download`, `/file`, `/reviews`, `/api/assistant`, `/api/semesters`), R2 and the seat-alert cron are live, and the Worker suite runs in CI (`npm run test:worker`) behind sync checks for its generated catalogue, review corpus and campus map. Remaining: the Cloudflare Vite integration, typed env bindings (`worker/index.js` is still plain JS), and hosting itself — the site is served by GitHub Pages, not Cloudflare. |
| 9 | Runtime config & seed data | Typed validated config module; safe fork/PR placeholders; Vite-native typed injection; faculty/review seed parity; lazy-load big datasets; parity tests before removing injection | 🟡 **in progress** — the typed, validated config module landed with 5G-a, fork-safe placeholders live in `runtime-config.template.js`, and the shell serves the generated file as `/runtime-config.js` (a comment stub when it is absent, so fresh clones build and stay cleanly offline). Remaining: seed data is still injected by `build3.py` into the `SEEDED_REVIEWS` / `SEEDED_FACULTY_PROFILES` placeholder arrays rather than imported Vite-natively, and the large datasets are not lazily loaded. |
| 10 | Security / perf / a11y | Audit dynamic HTML; remove unsafe innerHTML; preserve CSP + upload limits; URL/redirect validation; rate limits; server admin checks; route splitting + lazy heavy libs; budgets; axe on critical routes | 🟡 **in progress** — CSP hardening survives with two guards (`npm run test:csp` for inline handlers, plus the workflow's own assertion that `unsafe-inline` is gone from `script-src` and `frame-ancestors` is present in all three bundles), routes are code-split with the heavy libraries loaded on demand, upload limits and admin checks are server-side, and axe runs over the shell routes (`e2e-shell/a11y-routes.spec.js`, serious/critical failing the build). Remaining: performance budgets — the Vite `main` chunk is 953 kB (276 kB gzip) as of 2026-09-08, over Vite's own 500 kB warning and up from the 796 kB recorded at the Phase 0 baseline, so it is growing unwatched — and a systematic audit of the remaining dynamic-HTML/`innerHTML` sites. |
| 11 | Testing & observability | TS/lint/domain/RTL/user-event/jest-dom/emulator/Worker/contract/Playwright/a11y/bundle-smoke; request+correlation IDs; structured logs; user-safe codes; queue/Worker/upload/auth metrics; frontend error reporting | 🟡 **in progress** — the test matrix *is* CI: lint, format, typecheck, `validate:data`, unit + Firestore-rules (emulator), Worker, legacy E2E, standalone-pages E2E, bundle smoke, the CSP guard, shell build + shell E2E, and the visual parity gate (which re-authors legacy baselines in-environment, since screenshots are not portable between machines). A structured logger and global error handlers ship in `src/platform/observability/`, and every routed Worker response carries an `X-Request-Id` matching its own logs. Remaining: a frontend error *sink* — the handlers exist but nothing receives what they catch — and queue/upload/auth metrics. |
| 12 | Vite production deploy | Verified multi-file `dist/`; entry points + asset paths + Firebase chunk isolation + runtime config + R2/Worker routes; GH Actions deploy; Cloudflare preview env; full E2E on preview; manual mobile/desktop; rollback verified | ⬜ — CI builds the shell and runs its full E2E suite on every PR, but that step says plainly that the output is **not deployed**. GitHub Pages still serves the `build3.py` bundle, so none of this phase's exit criteria (entry points, asset paths, deploy workflow, preview environment, verified rollback) has been exercised against a real deployment. |
| 13 | Production cutover | Legacy-vs-new parity; all flows + existing localStorage + cloud restore + signed in/out + Firebase-down + corrupt-state + mobile + prod CSP + admin + cron; rollback tag + documented rollback command; switch to Cloudflare; monitor; keep legacy recoverable | 🟡 **in progress** — parity audit done (#460): the shell e2e suite is green and every legacy surface maps to a working shell route, with two gaps closed first — the **Course Difficulty Map** was ported to a real `/difficulty` route (#461) and **Admin** stays the standalone `admin.html` (the shell nav links out to it; the stub route was dropped). The cutover itself is deploy wiring only: the shell builds at base `/Shohoj/` and becomes the deploy root, the vanilla site is retained (recoverable) at `/Shohoj/legacy/` together with `profile.html` and the standalone public pages so nothing shadows a shell route, the retired `/app/` beta redirects (bare URL via a stub, deep links rewritten by `404.html`), and `404.html` now hands every missing path to the SPA except the static `/admin/` and `/legacy/` trees. Verified against a locally assembled deploy folder served through a GitHub-Pages emulator (root, deep-link hard loads, `/app/` bookmarks, `/legacy/`, `/admin/`). Rollback documented in `docs/ROLLBACK.md` (Option C) behind the `legacy-single-file-build` tag. **Correction — this row used to end "merge = the actual flip".** It was merged, and then taken out: #460 shipped the root cutover, #465 reverted it, it was redone, and it was reverted again because the shell's route interiors were still under-styled. `docs/ROLLBACK.md` Option C is the account, and the two `Revert` commits are on `main`. The deployed root is the vanilla site today. What stands between here and a third attempt is the route-interior parity punch list — `npm run parity:report` is the trustworthy measurement of it, and it runs advisory on every PR. Post-deploy smoke, monitoring and Cloudflare all remain after that |
| 14 | Legacy retirement | After a stable period: remove build3.py, legacy modules, unused `window._shohoj_*`, island flags, old plugins; dead-code analysis; update docs/README/CHANGELOG | ⬜ |

## What is actually left (2026-09-08)

The table above is long because each row carries the record of how it was closed. The
short version, for anyone deciding what to pick up:

| Open work | Where it is tracked |
|---|---|
| Route-interior visual parity — the last thing between the shell and a third cutover attempt | Phase 13; measure with `npm run parity:report` |
| Deploy the shell at all (entry points, asset paths, preview env, verified rollback) | Phase 12 — blocked behind the above |
| Cloudflare Vite integration + typed Worker env bindings; hosting is still GitHub Pages | Phase 8 |
| Vite-native seed injection (still `build3.py` placeholder arrays) + lazy-loading the big datasets | Phase 9 |
| A performance budget for the Vite `main` chunk (953 kB, and growing); the dynamic-HTML audit | Phase 10 |
| A frontend error sink (handlers exist, nothing receives them); queue/upload/auth metrics | Phase 11 |
| The three stricter TS flags deferred at Phase 2 and never landed | #658 (one PR per flag; 15 / 117 / 213 errors) |
| Legacy retirement | Phase 14 — deliberately last, and only after a stable period |

Two things that look like migration work in these rows are **not**: the Firestore rules
divergence is an ops failure (#651, the deploy job is failing), and the CONNECT/semester
feed work is product work that happened to land during the migration.

## Phase 1 detail — as shipped

> Written as the Phase 1 PR description; kept as the record of what that phase did. All four items are in `main`.

**Priorities (in order):** production-bundle identifier-collision detection → fix confirmed
unsafe collisions → production-bundle regression coverage → migration-document reconciliation.

1. `scripts/check_bundle_collisions.py` — imports `build3.py`'s canonical file lists +
   strip logic, finds top-level (column-0) declarations duplicated across a page's modules,
   fails on any not in a documented `ALLOWLIST` (intentionally-identical helpers only).
2. Fix the 8 unsafe `MAIN` collisions by namespacing module-private helpers:
   `js/ui/groupsTab.js` → `_grp*`, `js/ui/reviewsTab.js` → `_rvt*`. No cross-file or
   `window.*` references touched (verified). `normalizeCourseCode` / `isKnownCourseCode`
   allowlisted (identical bodies) pending shared-module extraction in Phase 6.
3. Regression coverage: `npm run check:collisions` wired into CI before `build3.py`; the
   existing `test:bundle` smoke continues to exercise the built page at runtime.
4. Docs: this `docs/architecture/` set; superseded banners added to stale docs.

**Out of scope for Phase 1:** any React/Vite cutover, state rewiring, new features, CSS, or
Firestore/Worker changes. Those are Phases 2+.

## Phase 5C detail — typed catalogue + accessible autocomplete

**Scope:** replace ONLY the course-catalogue placeholders on the shell `/calculator`
route (`catalog: []`, `isKnownCode: () => false`) and ship a production-quality,
accessible autocomplete. No other Phase 5 work.

1. **One catalogue, no duplication.** `src/features/calculator/catalog.ts` imports the
   shipping `ALL_COURSES` from `js/core/catalog.js` through `js/core/catalog.d.ts`,
   validates + dedupes + freezes it once at module load. The bridge now supplies the real
   `BRACU_COURSE_CATALOG` + `isKnownCourseCode`. See
   `docs/architecture/decisions/0001-calculator-catalogue-search-boundary.md`.
2. **Pure deterministic search.** `courseSearch.ts` ranks matches in a fixed,
   locale-independent order (exact-code → code-prefix → code-substring → exact-title →
   title-prefix → title-substring), over a precomputed immutable view (`prepareCatalog`)
   so per-keystroke search never re-normalises the catalogue. De-duped, capped, no mutation.
3. **Pure selection invariants.** `courseSelection.ts` owns the grade-reset-on-identity-
   change rules (selecting a course fills canonical identity + official credits; an
   identity change clears stale grade/grade-point; re-picking the same course preserves
   grades; unknown free text never inherits credits) — lifted out of the JSX.
4. **Accessible combobox.** `CourseNameInput.tsx` implements the WAI-ARIA combobox/listbox
   pattern (`role`, `aria-expanded/controls/activedescendant`, `aria-selected`, keyboard
   nav, mouse-down selection, Escape, blur safety). Suggestion UI state never enters
   academic state; persistence stays on the Phase 4 engine.
5. **Tests:** pure units (search ranking/normalisation/dedupe/limit/mutation-safety,
   selection invariants, catalogue adapter) + shell E2E (search/select by mouse and
   keyboard, Escape, reload-restore, unknown-input safety, axe).

**Out of scope for 5C** (each of these was a later Phase 5 slice, and every one of them
has since shipped — see the Phase 5 row; only the production cutover is still open, as
Phase 13): demo mode, faculty-rating flow, full GPA/CGPA results UI, summary/dashboard
parity, simulator, grade changer, reverse solver, retake strategy, transcript import, PDF
export, planner integration, cloud restore/conflict parity, full mobile parity, final a11y
parity, production cutover. No `build3.py`, legacy, CSP, island, or Firestore changes.

## Phase 5D detail — pure results model + bridge-driven results UI

**Scope:** compute and render the CGPA results on the shell `/calculator` route —
headline, meter, academic standing, incomplete-grades warning, credit totals — from the
injected bridge only. No other Phase 5 work.

1. **One results model.** `src/features/calculator/results.ts` mirrors every computation
   `recalc()` (js/main.js) makes before touching the DOM: projected/completed CGPA,
   headline label, meter percent + status kind (including the recovery status's
   projected-figure quirk, mirrored deliberately), standing cutoffs (BRACU Summer 2022+
   probation policy), incomplete-semester counting, credit totals. Pure, presentation-free,
   node-testable.
2. **Three consumers, one definition.** The composed shell section
   (`CalculatorResults.tsx`) renders from the model over `useCalculatorBridge()`; the
   existing `CgpaSummary` / `CgpaMeter` / `CgpaCreditTotals` islands were refactored onto
   the same model via the default `legacyWindowBridge`, deleting their duplicated
   threshold logic while keeping rendered output identical. Meter/standing wording lives
   once, in `CalculatorResults.tsx`; headline colors reuse `gpaBadgeColors`.
3. **Core resolvability.** `src/core/gpa.ts`/`types.ts` imports gained explicit `.ts`
   extensions (the node-runner convention) so unit tests can traverse the GPA core; the
   typed-core parity transpiler maps them.
4. **Tests:** model units (every meter/standing threshold at exact boundaries, headline
   label, incomplete counting, credit formatting) + shell E2E (empty invite state, live
   results after grading, reload-recompute, incomplete warning, axe scan).

**Out of scope for 5D** (all since delivered except the cutover, which is Phase 13): demo
mode, faculty-rating flow, add-semester/running-semester controls on the shell, degree
tracker, GPA trend chart, simulator, grade changer, reverse solver, retake strategy,
transcript import, PDF export, planner integration, cloud restore/conflict parity, full
mobile parity, final a11y parity (the shell shipped no visual system when this was
written — it has one now, tokenized and themed), production cutover. No `build3.py`,
legacy, CSP, or Firestore changes.

## Guardrails (every phase)

No push to `main`; no force-push without fetch; no giant migration commit; no deleting
working legacy code before parity; no silent Firestore/user-data changes; no secrets in
client bundles; no trusting client role checks; no unsafe raw HTML; no weakening rules/CSP;
never commit generated `dist/`/`shohoj.html`/`admin.html`; atomic one-file-per-commit;
explicit staging; no AI attribution trailers; "Part of #N" not auto-closing keywords.
