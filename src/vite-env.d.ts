// Vite client types (import.meta.env, asset imports) for the TS sources — the
// router reads import.meta.env.BASE_URL for its basename (#449). tsconfig sets
// "types": [], so this reference is the single opt-in.
/// <reference types="vite/client" />
