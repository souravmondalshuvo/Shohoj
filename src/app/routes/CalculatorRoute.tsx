// src/app/routes/CalculatorRoute.tsx
//
// Calculator route (Phase 3 skeleton). Lazy route module. A later Phase 5
// increment mounts the existing React calculator (src/features/calculator) here
// once it reaches parity; for now this is a placeholder that preserves the route
// in the map without claiming behavior it doesn't yet have.

export function Component() {
  return (
    <section className="shell-page">
      <h1>CGPA Calculator</h1>
      <p className="shell-muted">
        The calculator migrates here in Phase 5. Until then it lives in the legacy
        page and the opt-in React island.
      </p>
    </section>
  );
}
