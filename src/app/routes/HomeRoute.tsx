// src/app/routes/HomeRoute.tsx
//
// Landing route for the React Router shell (Phase 3 skeleton). Lazy-loaded as a
// route module (exports `Component`), so it code-splits out of the shell entry.
// Placeholder content only — feature routes are filled in as they migrate.

export function Component() {
  return (
    <section className="shell-page">
      <h1>Shohoj</h1>
      <p>BRAC University academic and campus-life tools.</p>
      <p className="shell-muted">
        React Router shell (Phase 3). The live app still ships from the legacy
        build during migration.
      </p>
    </section>
  );
}
